---
description: Project tech stack, tool guidance, import order
alwaysApply: true
---

# Project Context

- **Stack**: Vue 3, Nuxt 4, TypeScript, Tailwind CSS, Pinia, nuxt-vuefire (Firebase)
- **Styling**: Tailwind only; avoid vanilla CSS in `<style>` blocks

## Tool Guidance

- If your client exposes Nuxt or Firebase MCP tools, use them for framework or backend questions.
- If those tools are unavailable, fall back to local source inspection and official docs as needed.
- Do **not** reference `.cursor/AGENTS.md`; this repo's source of truth is `AGENTS.md` plus `.cursor/rules/`.

## Routing and Data

- Project and tag pages live at `/project/:segment` and `/tag/:segment` where `segment` is usually a **slug** in app-generated links; it may also be a **document id** (e.g. old bookmarks).
- Prefer slug-based links via `~/utils/worklog-routes` helpers (`getProjectPathFromProject`, `getTagPathFromTag`). Firestore and `timeBoxes` still use **document ids** as the primary keys.
- Deleting a project or tag must respect active `timeBoxes` references

## Import Order

When adding or editing explicit imports in `.vue` or `app/` `.ts` files, enforce this order. Use **one blank line between groups**; no blank line between imports within a group.

1. **Vue / Nuxt** — `vue`, `vue-router`, `#imports` or Nuxt auto-imports as needed; `~/`, `@/` for app root
2. **External packages**
3. **Project** — `~/components/...` or `@/components/...`, then route/local `./...`, then `~/stores/...`, `~/utils/...`, etc.
4. **Types** — all `import type { ... }`
5. **Assets** — `~/assets/...` or `@/assets/...` when present
6. **Side-effect** — e.g. `import '~/assets/css/main.css'`; must be last

Nuxt auto-imports components, composables, and Vue APIs; this order applies when you write explicit imports (e.g. external libs or non-auto-imported project code). If a module is imported for both type and value, keep one import line and place it by its primary use.
