rules_version: '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development: allow all for timeBoxes, projects, tags. Tighten when auth is added.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}